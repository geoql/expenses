import {
  app,
  css,
  modules,
  nitro,
  plugins,
  runtimeConfig,
  ssr,
  typescript,
} from './config';

export default defineNuxtConfig({
  app,
  css,
  ssr,
  modules,
  nitro,
  plugins,
  runtimeConfig,
  typescript,
});
